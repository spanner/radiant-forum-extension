- topic ||= @topic

- if topic.replied_by
  = t('forum.post_count_from', :count => topic.posts.count - 1)
  = link_to topic.replied_by.name, reader_url(topic.replied_by)
  = t('forum.on_date', :date => friendly_date(topic.replied_at))
