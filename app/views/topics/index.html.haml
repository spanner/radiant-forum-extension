= render :partial => 'readers/flasher'
= render :partial => 'readers/controls'

#forum
  - if @topics.empty?
    %p No talk here yet.

  - else
    %h1 All Discussions
    %ul.biglist
      - @topics.each do |topic|
        = render :partial => 'summary', :locals => {:topic => topic}
