- post ||= @post
.post_removal
  %p.warning
    = t('forum.confirm_removal_of', :author => post.reader.name)
  %p.buttons
    - form_for post, :html => {:method => 'delete', :class => 'friendly'} do |f|
      = f.submit t('forum.definitely_remove_post')
      = t('forum.or')
      = link_to t('forum.cancel'), paginated_post_url(post), :class => 'cancel'
  
